[rest]
bindAddr = "192.0.2.10:13301"
apiKey = "change-me"
# Optional. When unset, the daemon serves `webui` beside emulebb-rust.exe if
# that directory exists. Relative paths are resolved from the profile directory.
# webRootDir = "webui"
